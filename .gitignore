node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# Disalin otomatis oleh scripts/copy-pdf-worker.mjs saat dev/build.
public/pdf.worker.min.mjs

# Kredensial deploy lokal (Vercel/GoDaddy), jangan pernah di-commit.
.env.deploy

# Salinan konflik dari sinkronisasi berkas (iCloud/Finder).
* 2.*
* 2/
